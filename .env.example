DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-id/your-token
POLL_INTERVAL_SECONDS=30

# Inside Docker this is host.docker.internal so the container can reach TTR on your PC.
# For `dotnet run` on the host, leave unset (defaults to 127.0.0.1).
# COMPANION_HOST=127.0.0.1
